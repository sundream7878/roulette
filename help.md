# 네이버 카페 댓글 수집 시스템 - 현재 상황 정리

## 🔍 문제 분석 결과

### 실제 상황
- **웹 페이지에서 보이는 댓글**: 44개
- **API가 반환하는 댓글**: **12개만**
- **수집된 참가자**: 12명 (정확함)

### 핵심 발견사항

**네이버 카페의 모든 공개 API가 전체 댓글을 제공하지 않습니다.**

테스트한 API 목록:
1. ✗ `cafe-articleapi/v2` → 12개만 반환
2. ✗ `cafe-articleapi/v2.1` → 500 에러
3. ✗ Article Detail API → 500 에러  
4. ✗ `CommentList.json` → API 존재하지 않음

**결론**: 현재 사용 가능한 공개 API로는 전체 44개 댓글을 가져올 수 없습니다.

## 🚫 왜 API가 일부만 반환하나요?

네이버 카페는 다음과 같은 이유로 API를 제한합니다:

1. **보안 정책**: 로그인하지 않은 사용자에게는 제한된 데이터만 제공
2. **서버 부하 방지**: 대량의 데이터 요청 차단
3. **크롤링 방지**: 자동화된 데이터 수집 제한

## 💡 해결 방법

### 방법 1: 브라우저 자동화 (Selenium/Playwright)
**장점**: 실제 브라우저처럼 작동하여 모든 댓글 접근 가능
**단점**: 느리고, 로그인 필요할 수 있음, 감지될 위험

```python
# 예시 (구현 필요)
from selenium import webdriver

driver = webdriver.Chrome()
driver.get(url)
# "댓글 더보기" 버튼 클릭 반복
# 모든 댓글 요소 추출
```

### 방법 2: 로그인 쿠키 사용
**장점**: API 접근 권한 증가 가능
**단점**: 쿠키 관리 복잡, 계정 정지 위험

```python
# 예시
headers = {
    "Cookie": "NID_AUT=...; NID_SES=...",  # 브라우저에서 복사
}
```

### 방법 3: 현재 상태 유지 (12개 제한 수용)
**장점**: 안정적, 간단
**단점**: 전체 댓글 수집 불가

**추천**: 이벤트 참여자가 보통 10명 내외라면 현재 방식으로도 충분할 수 있습니다.

## 📊 현재 시스템 상태

### 정상 작동하는 기능
- ✅ API를 통한 댓글 수집 (12개 제한)
- ✅ 중복 제거 및 카운팅
- ✅ `participants.txt` 저장
- ✅ 룰렛 게임 반영

### 제한사항
- ❌ 12개 이상의 댓글 수집 불가 (API 제한)
- ❌ 전체 참가자 목록 수집 불가

## 🎯 다음 단계 선택지

### A. 브라우저 자동화 구현
- Selenium 또는 Playwright 사용
- 모든 댓글 수집 가능
- 구현 복잡도: 높음
- 예상 시간: 2-3시간

### B. 현재 상태 유지
- 12개 제한 수용
- 추가 작업 불필요
- 작은 이벤트에는 충분

### C. 수동 입력 방식 추가
- 웹 페이지에서 복사 → 붙여넣기
- 간단하고 확실
- 매번 수동 작업 필요

## 📝 사용자 결정 필요

어떤 방향으로 진행하시겠습니까?

1. **브라우저 자동화 구현** (모든 댓글 수집)
2. **현재 상태 유지** (12개 제한 수용)
3. **수동 입력 기능 추가** (복사/붙여넣기)

선택하신 방향에 따라 추가 개발을 진행하겠습니다.

## 🧹 데이터 섞임 및 초기화 문제 해결 (2.13 업데이트)

많은 시도 끝에 데이터가 계속 섞이거나 이전 글의 데이터가 보이는 현상을 방지하기 위해 다음 장치들을 마련했습니다.

### 1. 주요 원인 및 조치 사항
- **URL 정규화 (Canonical URL)**: 네이버 카페는 동일한 글도 주소 형태가 여러 가지(모바일, PC, 별명 등)일 수 있습니다. 이를 모두 하나의 표준 형태로 변환하여 DB에 저장함으로써 데이터가 분산되거나 섞이는 것을 원칙적으로 차단했습니다.
- **세션 초기화 (Session Clear)**: 새로운 수집을 시작할 때마다 내부 통신 세션을 초기화하여 이전 게시글의 상태가 전이되지 않도록 했습니다.
- **디버깅용 응답 ID (Response ID)**: 이제 모든 서버 응답에는 고유 ID가 부여됩니다. 브라우저 개발자 도구(F12) 콘솔에서 `DEBUG: API Response Received` 로그를 통해 현재 화면에 보이는 데이터가 언제 생성된 것인지 정확히 확인할 수 있습니다.

### 2. 문제가 지속될 때 확인 체크리스트
1. **서버 재시작**: `comment_dart.py`가 실행 중인 터미널에서 `Ctrl+C` 후 다시 실행하여 최신 로직(정규화 등)이 활성화되도록 합니다.
2. **웹 페이지 새로고침 (F5)**: 브라우저 캐시 문제를 방지하기 위해 새로고침을 수행합니다.
3. **'✨ 새 이벤트 생성' 버튼 사용**: 이 버튼은 UI와 입력값을 완전히 비우고 새로운 수집을 위해 준비하는 안전한 시작점입니다.
4. **브라우저 콘솔 확인**: F12를 눌러 `response_id`가 매번 바뀌는지, 그리고 `Total: 6`과 같이 서버 로그와 일치하는지 확인합니다. 만약 서버 로그는 6인데 화면에 21이 나온다면, 브라우저가 이전 데이터를 메모리에 들고 있는 것이므로 강력 새로고침(Ctrl+F5)이 필요합니다.

### 3. 기술적 상세 로그
- `monitor_view.py`: `fetch_comments`와 `load_comments` 모두에서 URL 표준화 로직 적용 완료.
- `scraper.py`: `get_new_comments` 시작 시 세션 강제 클리어 로직 추가.

## 🛠️ 현재 미해결 및 작업 필요 사항 (TODO)

### 1. 실시간 업데이트 안정성 (Socket.io)
- **현상**: `index.html` 내의 소켓 핸들러가 중복 등록되는 구조적 결함이 발견됨.
- **영향**: 게시글 상태를 확인할 때마다 업데이트 이벤트가 중복 처리되어 브라우저 부하가 늘어나거나 차트가 깜빡일 수 있음.
- **해결 필요**: 핸들러를 바깥으로 빼서 한 번만 등록되도록 수정 필요.

### 2. 다중 이벤트 간섭 문제
- **현상**: 모든 모니터링 결과가 `participants.txt`라는 단일 파일에 저장됨.
- **영향**: 두 개 이상의 게시글을 동시에 열어두고 수집하면, 룰렛 화면에는 마지막으로 수집된 게시글의 데이터만 보임.
- **해결 필요**: 이벤트 ID별로 별도의 파일 생성 또는 메모리 기반 실시간 통신 강화 필요.

### 3. Selenium 수집기의 불안정성
- **현상**: 네이버의 로그인 방식 변경이나 DOM 구조 변경 시 Selenium 방식이 실패할 수 있음.
- **영향**: API (12개 제한)로만 수집될 경우 12명 이상의 참가자를 누락하게 됨.
- **진행 상황**: 현재 `diagnostic.py`를 통해 수동으로 점검 중이며, 자동 복구 로직 보강 필요.

### 4. '완전 강제 초기화' 기능 부재
- **현상**: DB와 메모리, 파일을 모두 한 번에 깨끗이 비우는 기능이 UI에 없음.
- **영향**: 이전 게시글의 흔적(21개 vs 6개 등)이 남는 근본적인 원인이 됨.
- **해결 필요**: 서버 측 세션과 DB, 파일을 일괄 삭제하는 '강제 리셋' 버튼 추가 권장.

### 5. 모바일 URL 대응 미흡
- **현상**: 모바일 버전 URL(`m.cafe.naver.com/...`) 입력 시 일부 파싱 오류 가능성.
- **해결 필요**: `parsers.py`의 정규식 보완 필요.

